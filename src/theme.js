// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#ff268e',
    },
    secondary: {
      main: '#ff694f',
    },
  },
});

export default theme;